extends: spectral:oas
rules:
  no-delete-operations:
    description: "DELETE operations are not allowed."
    severity: error
    # JSONPath to find any 'delete' operation under paths
    given: "$.paths[*][?(@.delete)]"
    then:
      field: "delete"
      action:
        function: falsy # This ensures the field should not exist
